
@{
    ViewBag.Title = "_RoleDropdownsPartial";
}

@if (Request.IsAuthenticated && (User.IsInRole("Employee") || User.IsInRole("Admin")))
{
    <ul class="nav navbar-nav justify-content-start">
        <li class="dropdown nav-item">
            <a href="#" data-toggle="dropdown" role="button" aria-haspopup="true"
               aria-expanded="false" class="dropdown-toggle nav-link">Narzędzia<span class="caret"></span></a>
            <ul class="dropdown-menu">
                <li class="dropdown-item">
                    <a href="~/Areas/Worker/WorkerPanel.aspx">Panel pracownika</a>
                </li>
            </ul>
        </li>
    </ul>
}

@if (Request.IsAuthenticated && (User.IsInRole("Owner") || User.IsInRole("Admin")))
{
    <ul class="nav navbar-nav justify-content-start">
        <li class="dropdown nav-item">
            <a href="#" data-toggle="dropdown" role="button" aria-haspopup="true"
               aria-expanded="false" class="dropdown-toggle nav-link">Narzędzia szefa<span class="caret"></span></a>
            <ul class="dropdown-menu">
                <li>@Html.ActionLink("Role Manager", "Index", "Roles")</li>
                <li class="dropdown-item">Raporty</li>
                <li class="dropdown-item">Dostawy</li>
            </ul>
        </li>
    </ul>
}

@if (Request.IsAuthenticated && User.IsInRole("Admin"))
{
    <ul class="nav navbar-nav justify-content-end">
        <li class="dropdown nav-item">
            <a href="#" data-toggle="dropdown" role="button" aria-haspopup="true"
               aria-expanded="false" class="dropdown-toggle nav-link">Admin Isle<span class="caret"></span></a>
            <ul class="dropdown-menu">
                <li class="dropdown-item">
                    <a href="#">Manage users</a>
                </li>
            </ul>
        </li>
    </ul>
}